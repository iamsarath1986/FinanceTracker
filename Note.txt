python3 -m venv .venv
source .venv/bin/activate

Docker
======
    docker compose up -d              # start everything
    docker compose down               # stop (data volume persists)
    docker compose down -v            # stop + wipe DB volume
    docker compose logs -f backend    # tail backend logs

rebuild will not delete your database data.

  Safe commands:

  docker compose up -d --build
  docker compose up -d --force-recreate
  docker compose restart

  Your Postgres data is stored in the named Docker volume:

  volumes:
    - postgres_data:/var/lib/postgresql/data

  Data is lost only if you remove that volume, for example:

  docker compose down -v

  or manually delete the Docker volume.

  So for rebuilds, use:

  docker compose up -d --build

  Avoid docker compose down -v unless you intentionally want a fresh
  empty database.


Database (PostgreSQL)
=====================

Each time we add a model (accounts, transactions, budgets, etc.), the workflow will be:

  # 1. Write the SQLAlchemy model in app/models/
  # 2. Generate the migration
  docker compose exec backend alembic revision --autogenerate -m "add accounts table"
  # 3. Apply it
  docker compose exec backend alembic upgrade head

  Alembic commands:

  # Generate a new migration
  docker compose exec backend alembic revision --autogenerate -m "add categories table"

  # apply it
  docker exec -T backend alembic upgrade head

  # Restart backend
  docker compose restart backend

  # Roll back the last migration
  docker compose exec backend alembic downgrade -1

  # See current migration status
  docker compose exec backend alembic current


Access pgsql shell
==================
docker exec -it financetracker-db-1 psql -U financeuser -d financedb

  Once inside, useful commands:

  -- list all tables
  \dt

  -- check accounts table structure
  \d accounts

  -- see all data
  SELECT * FROM accounts;

  -- exit
  \q

API Endpoints
=============
http://localhost:8001/auth/login
http://localhost:8001/accounts
